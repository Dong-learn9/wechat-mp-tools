# Docker 专用依赖清单（排除桌面端 pywebview / Windows / macOS 专有库）
# Playwright 锁定 1.48.0：对应 chromium v1148 在国内镜像源完整可用
flask>=3.0
flask-cors>=4.0
requests>=2.31
curl_cffi>=0.7
cryptography>=42.0
playwright==1.48.0
scrapling>=0.2
pysocks>=1.7
xhshow>=0.2.0
pyyaml>=6.0
httpx
openpyxl>=3.1
